import 'package:flutter/material.dart';

class DailyReportScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Daily Attendance Report'),
      ),
      body: Center(
        child: Text('Custom Daily Attendance Report'),
      ),
    );
  }
}
